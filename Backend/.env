# Database connection settings
DB_HOST=localhost
DB_PORT=5432
DB_USER=root
DB_PASSWORD=******
DB_NAME=boom

# Complete connection string (optional - can be constructed in code if needed)
DATABASE_URL=postgresql://${DB_USER}:${DB_PASSWORD}@${DB_HOST}:${DB_PORT}/${DB_NAME}
